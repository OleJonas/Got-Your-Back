/**
 * @module sensorPlacement
 * @category Utils
 */

/**
 * @returns {{}} A json-object with sensor placements as values based on associated sensor index as key.
 */
export const sensorPlacement: any = {
  1: "Lower neck",
  2: "Lumbar spine",
  3: "Right shoulder",
};
export default sensorPlacement;
